<%= form_for @post do |t| %>
    <%= t.label :title %>: 
    <%= t.text_field :title, :id => "post_title" %><br>

    <%= t.label :content %>: 
    <%= t.text_field :content, :id => "post_content" %><br><br>
    
     <%= e.collection_select :category_id, @categories, :id, :name %><br><br>

    <%= f.label :new_category, "New Category:" %>
    <%= f.text_field :cateogry_id, :name => "new_cateogry", :id => :new_cateogry %>   

    <%= t.submit %>
<% end %>